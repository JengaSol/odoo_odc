<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_commission_plan_form_partner" model="ir.ui.view">
        <field name="name">sale.commission.plan.form.partner</field>
        <field name="model">sale.commission.plan</field>
        <field name="inherit_id" ref="sale_commission.sale_commission_plan_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id'][@groups='base.group_multi_company']" position="replace">
                <field name="company_ids" widget="many2many_tags" options="{'no_create': True}" groups="base.group_multi_company" readonly="state != 'draft'"/>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_refresh_commissions" type="object"
                        class="oe_stat_button" icon="fa-refresh"
                        invisible="user_type != 'partner' or state != 'approved'">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Update Commissions</span>
                    </div>
                </button>
            </xpath>
            <xpath expr="//notebook[@name='main_book']" position="inside">
                <page string="Agents" invisible="user_type != 'partner'">
                    <field name="partner_ids">
                        <list editable="bottom">
                            <control>
                                <create string="Add a new Agent"/>
                            </control>
                            <field name="partner_id"/>
                            <field name="date_from"/>
                            <field name="date_to"/>
                        </list>
                    </field>
                    <button string="Add Multiple Agents" type="action" name="%(sale_commission_partner.action_sale_commission_add_multiple_partner)d"/>
                    <button name="action_cleanup_orphan_agents" type="object"
                            string="Remove deleted agents" class="btn-secondary"
                            invisible="user_type != 'partner'"
                            confirm="Remove agent assignments linked to deleted contacts?"/>
                </page>
            </xpath>
            <xpath expr="//field[@name='user_ids']/.." position="attributes">
                <attribute name="invisible">user_type == 'partner'</attribute>
            </xpath>
        </field>
    </record>

    <record id="sale_commission_plan_list_partner" model="ir.ui.view">
        <field name="name">sale.commission.plan.list.partner</field>
        <field name="model">sale.commission.plan</field>
        <field name="inherit_id" ref="sale_commission.sale_commission_plan_view_list"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="replace">
                <field name="company_ids" widget="many2many_tags" groups="base.group_multi_company"/>
            </xpath>
        </field>
    </record>
</odoo>
